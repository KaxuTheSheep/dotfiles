add_newline = false
palette = "amethyst"

# ─── Palette ─────────────────────────────────────────────────
[palettes.amethyst]
accent = "#7766FF"
bg     = "#04040A"
fg     = "#C8B8FF"
dark   = "#2A1B5E"
mid    = "#3D2B8A"
error  = "#FF4466"

# ─── Format ──────────────────────────────────────────────────

format = """\
 $directory$git_branch$git_status
$character\
"""
right_format = """\
$c$cpp$java$python$rust$nodejs $time\
"""

continuation_prompt = '[▶▶](fg:accent) '

# ─── Operating system ────────────────────────────────────────
[os]
disabled = false
format = "[ $symbol ]($style)"
style = "bold fg:accent"
[os.symbols]
Fedora= " "

# ─── Directory ───────────────────────────────────────────────
[directory]
disabled = false
format = "[$path ]($style)"
style = "bold fg:fg"
truncate_to_repo = false
truncation_length = 3
truncation_symbol = "…/"

[directory.substitutions]
"Documents" = "󰈙 "
"Downloads" = " "
"Pictures"  = "󱢉 "
"Music"     = " "

# ─── Git ─────────────────────────────────────────────────────
[git_branch]
format = "[ $symbol$branch]($style)"
style = "bold fg:accent"
symbol = " "

[git_status]
format = '([$all_status$ahead_behind ]($style))'
style = "bold fg:accent"
ahead = '⇡${count}'
behind = '⇣${count}'
diverged = '⇕⇡${ahead_count}⇣${behind_count}'
modified = '!${count}'
untracked = '?${count}'
staged = '+${count}'
deleted = '✘${count}'

# ─── Character ───────────────────────────────────────────────
[character]
success_symbol = '[❯](bold fg:accent)'
error_symbol   = '[❯](bold fg:error)'

# ─── Languages ───────────────────────────────────────────────
[c]
format = "[ $symbol]($style)"
style = "bold fg:fg"
symbol = " "
detect_extensions = ["c", "h"]

[cpp]
format = "[ $symbol]($style)"
style = "bold fg:fg"
symbol = " "
detect_extensions = ["cpp", "hpp", "cc", "cxx"]

[java]
format = "[ $symbol]($style)"
style = "bold fg:fg"
symbol = " "

[python]
format = "[ $symbol($virtualenv )]($style)"
style = "bold fg:fg"
symbol = " "
version_format = "${raw}"

[rust]
format = "[ $symbol]($style)"
style = "bold fg:fg"
symbol = " "

[nodejs]
format = "[ $symbol]($style)"
style = "bold fg:fg"
symbol = " "
detect_extensions = ["js", "mjs", "cjs", "ts"]
detect_files = ["package.json", "package-lock.json", "yarn.lock"]
detect_folders = ["node_modules"]
version_format = "${raw}"

# ─── Disabled ────────────────────────────────────────────────
[aws]
disabled = true

[gcloud]
disabled = true
